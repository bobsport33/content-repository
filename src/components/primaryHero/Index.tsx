import React from "react";
import styled from "styled-components";

const PrimaryHeroContainer = styled.section``;

const PrimaryHero = () => {
    return <PrimaryHeroContainer></PrimaryHeroContainer>;
};

export default PrimaryHero;
